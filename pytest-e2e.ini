[pytest]
testpaths = tests/e2e
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
# E2E tests are slow; default to running serially (no parallelism).
# Browser launches dominate the runtime.
filterwarnings =
    ignore::DeprecationWarning