"""Auth tests: run with ``python -m backend.tests`` from the project root.

Tests use Flask's built-in test client + unittest, so no extra test
dependencies are required (the project only ships Flask).
"""
